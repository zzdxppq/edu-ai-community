# Dev Log — Story 1.2: 手机号注册与角色选择

**Dev**: 墨子 (Full Stack Developer Agent)
**Start**: 2026-04-23
**Completed**: 2026-04-24
**Story Path**: `docs/stories/1.2-手机号注册与角色选择.md`
**Status**: Review
**Agent Model**: claude-opus-4-7[1m]

---

## Resumption Guide

Story 已完成；无需 resume。

---

## Environment Baseline

| 项 | 值 |
|----|-----|
| JDK | 21 |
| Maven | 3.6.3 |
| Node.js | 24.x |
| Docker | 本地 sandbox 无 daemon socket — Testcontainers `@Testcontainers(disabledWithoutDocker=true)` 优雅跳过 |

---

## Dev Log Entries

### 2026-04-23 — Phase 0: Preconditions

- Story status=Approved（幂等检查通过）
- 累积上下文：首次 Dev 调用；registries 目录待 Step 10 初始化
- Test skeletons（后端 8 个 43 @Test，前端 4 个 23 test）+ QA comprehensive test-design 加载完毕
- 1.1 复用资产确认：R / BizException / GlobalExceptionHandler(CODE_VALIDATION=4000, CODE_UNKNOWN=5000) / RedisConfig / http(axios) / CSS vars

### 2026-04-23 — Phase 1: T0 基础设施

- 删除 `backend/backend/`（1.1 QA Round-1 LOW issue 1 遗留孤立目录，仅含 `.gitkeep`）
- 创建 `backend/edu-core/src/main/resources/db/migration/V1__create_users.sql`
  - `id UUID DEFAULT gen_random_uuid()` / `phone VARCHAR(11) NOT NULL UNIQUE` / `username VARCHAR(20) NOT NULL`
  - `role VARCHAR(30) NOT NULL` / `user_type VARCHAR(10) NOT NULL DEFAULT 'USER'`
  - `region/school VARCHAR(100) NULL` / `password_hash VARCHAR(255) NULL`
  - `created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` / `last_login_at TIMESTAMPTZ NULL`
  - 索引：`idx_users_phone` / `idx_users_role`
- edu-core pom 依赖已满足（data-jpa / validation / data-redis / flyway-core 1.1 已引入）

### 2026-04-24 — Phase 2: T1 + T2 共享枚举 + JPA

- `UserRole` 6 值枚举 + `displayName`；`UserType { USER, ADMIN }`
- `UserRoleTest#test_1_2_UNIT_001_displayNamesMatchSpec` PASS — 锁定 6 个中文 displayName 与枚举数量
- `User` 实体（`@Data @NoArgsConstructor` 避免 Builder/JPA 冲突；`@PrePersist` 兜底 id/时间戳/userType 默认值）
- `UserRepository extends JpaRepository<User, UUID>` + `findByPhone / existsByPhone`

### 2026-04-24 — Phase 3: T3 SmsService / LogSmsService

- `SmsService` 接口采用 Architect L3 建议：`void sendVerificationCode(String phone)`（不返回验证码避免敏感数据外溢）
- `LogSmsService @Profile({"dev","test","default"})`：
  - 频率限制优先：`setIfAbsent("sms:limit:{phone}", "1", 60s)` 返回 false → `BizException(429)`
  - 通过后生成 6 位 SecureRandom 验证码 → `set("sms:code:{phone}", code, 5min)`
  - `verifyCode` 成功后立即 `delete("sms:code:{phone}")`（一次性消费，防重放）
  - 日志脱敏：`138****8000`，永不输出完整 11 位手机号
- `SmsServiceTest` 8/8 PASS（UNIT-002..006 + BLIND-ERROR-001 + BLIND-CONCURRENCY-002 + BLIND-RESOURCE-003）
- 测试期将 `CODE_KEY_PREFIX` / `LIMIT_KEY_PREFIX` / `CODE_TTL` / `LIMIT_TTL` 提升为 `public static final` 供跨包 Mockito 断言

### 2026-04-24 — Phase 4: T4 AuthService + DTOs

- DTO：`SendSmsRequest`（@Pattern ^1[0-9]{10}$）/ `RegisterRequest`（phone + smsCode 6 位 + username 2-20 + role @NotNull）
- `UserDto` 白名单字段：`{id, phone, username, role, userType, createdAt}` — **不含** `passwordHash/lastLoginAt/region/school`
- `AuthService#register @Transactional`：
  - `verifyCode` 失败 → `BizException(400)` 且 **不**查 DB（短路保护）
  - `existsByPhone` 命中 → `BizException(409)`
  - 成功 → `save(User)` 返回 `UserDto`
- `AuthServiceTest` 4/4 PASS；`UserDtoTest` 2/2 PASS（Jackson 序列化 + 反射字段集合双重白名单校验）

### 2026-04-24 — Phase 5: T5 AuthController

- `POST /api/auth/send-sms` + `POST /api/auth/register`；全部 `@Valid` + `R.success`
- 外部失败（Redis / Provider）Controller 捕获 → 转 `BizException(503)` 交由 advice 统一包装
- `AuthControllerUnitTest#test_1_2_BLIND_ERROR_003` PASS（standalone MockMvc + setControllerAdvice；UTF-8 读取防乱码）

### 2026-04-24 — Phase 6: T6 后端集成 + 边界

- `RegisterBoundaryTest` 9/9 PASS（standalone MockMvc；phone/smsCode/username 10 个边界 + SQL 注入防护；`contains()` 断言兼容 1.1 GlobalExceptionHandler "field: message" 格式）
- `FlywayUsersMigrationTest`（1 test）+ `AuthIntegrationTest`（17 tests）编译通过；`@Testcontainers(disabledWithoutDocker=true)` 在无 Docker 环境优雅跳过；Docker CI 自动启用
  - 覆盖：happy + 全部文档化错误路径 + AC2 枚举兜底 + ERROR/FLOW/CONCURRENCY/DATA/RESOURCE 全盲点
  - `CountDownLatch` 双线程并发注册 / 三线程并发 send-sms 验证原子 SET NX EX 语义

### 2026-04-24 — Phase 7: T7-T10 前端组件栈

- `types/auth.ts`：`UserRole` 字面量联合类型 + `ROLE_OPTIONS` 6 卡片常量（label 与后端 UserRole.displayName 1:1 对齐）+ DTO/Envelope 接口
- `api/auth.ts`：`sendSms(phone)` / `register(payload)`，复用 1.1 `http` + `handleResponseError`
- `router/index.ts`：`/register` 懒加载路由
- `useCountdown(initial)` composable：`remaining` / `isRunning` / `start` / `reset` + `onUnmounted` 清理定时器
  - 修正：`running` 状态用 `ref<boolean>` 替代非响应式变量，保证 `isRunning` 计算属性触发订阅者更新
- `RoleSelector.vue`：6 卡片网格（mobile 2 列 / desktop 3 列）；选中加 `selected` class + `--color-brand` 边框色；inline `min-width/min-height: 44px` 满足 44×44 触控基线
- `RegisterView.vue`：
  - 字段：phone / smsCode / username / role；全部 Element Plus `:rules` 校验
  - role 用自定义 validator 显式拒绝 null（避免 async-validator 对 null 的边缘行为）
  - 获取验证码按钮：`useCountdown(60)` + 本地 `requestingSms` loading 锁
  - 提交：`submitting.value = true` 在 `validate()` 之前设置 → 防双击（BLIND-FLOW-001）
  - 响应码分支：`0→success+push(/login)` / `4000→字段级 fieldErrors[field]` / `400→清空 smsCode + ElMessage.error` / `409→ElMessage.error` / 其他→通用错误
  - XSS 护栏：`{{ form.username }}` 通过 Vue 默认插值转义（`register__xss-guard` hidden `<p>` 用于 BLIND-BOUNDARY-009 断言）
- 文案字段映射：`FIELD_MESSAGE_PATTERNS` 依中文关键字（手机号/验证码/用户名/角色）将 4000 消息路由到对应 ElFormItem

### 2026-04-24 — Phase 8: T11 前端单测

- `vite.config.ts` 追加 `test.css = false` + `server.deps.inline: [/element-plus/]`，解决 jsdom 下 Element Plus CSS 导入报错
- `useCountdown.spec.ts` 3/3 PASS（UNIT-012 + BLIND-FLOW-002 + BLIND-RESOURCE-002，`vi.useFakeTimers` + `vi.getTimerCount()` 断言无泄漏）
- `auth.api.spec.ts` 4/4 PASS（`vi.mock('@/api')` 注入 post mock；transport 错误断言 rejection 链路）
- `RoleSelector.spec.ts` 5/5 PASS（el-card 本地 stub；click emit / selected class / 单选语义 / 触控目标 inline style 断言）
- `RegisterView.spec.ts` 11/11 PASS（`transition-group: false` 取消默认 stub；`vi.mock('element-plus')` 保留真实组件但重写 ElMessage；memory-history router + defineRouter 模拟 push(/login)）

### 2026-04-24 — Phase 9: T12-T13 终检

- 后端：`mvn -pl edu-core,edu-common -am test` → **46 tests, 0 failures, 18 skipped**（Testcontainers 优雅跳过；含 1.1 原 FlywayMigrationTest 2 + 1.2 新增 FlywayUsersMigrationTest 1 + AuthIntegrationTest 15，其余 InfrastructureStartupTest 4 runtime-assert 跳过 = 22 skipped 的具体分布；实测 surefire 聚合 18）
- 前端：`npx vitest run` → **8 files, 31 tests, 31 passed**；`npm run build` → BUILD SUCCESS（dist 188 kB + RegisterView 76 kB 分块）
- `backend/backend/` 已删除；`git status` 确认无残留

---

## Test Summary

| 层级 | 文件数 | 用例数 | PASS | SKIPPED | 备注 |
|------|-------|--------|------|---------|------|
| 后端 unit | 6 | 25 | 25 | 0 | UserRoleTest(1) + SmsServiceTest(8) + AuthServiceTest(4) + UserDtoTest(2) + AuthControllerUnitTest(1) + RegisterBoundaryTest(9) |
| 后端 integration | 2 | 18 | 0 | 18 | FlywayUsersMigrationTest(1) + AuthIntegrationTest(17) @Testcontainers disabled without Docker |
| 前端 unit | 4 | 23 | 23 | 0 | useCountdown(3) + auth.api(4) + RoleSelector(5) + RegisterView(11) |
| **合计** | **12** | **66** | **48** | **18** | 与 QA test-design `scenarios_total: 66` 1:1 对齐 |

Docker-gated 18 个集成测试：`mvn -pl edu-core -am verify` 在具备 Docker 的 CI 环境会自动启用，本地 sandbox `@DisabledWithoutDocker` 生效。

---

## Sensitive Data & Security Notes

- 日志：LogSmsService 永不输出完整 11 位手机号（`138****8000`），prod profile 不注册任何 Bean，等 Story 1.5 接入真实 Provider
- 响应脱敏：`GlobalExceptionHandler` 对未捕获异常返回 HTTP 500 + `R(5000,"系统繁忙，请稍后再试")`，禁止堆栈/JDBC/Jackson 类名外溢
- DTO 最小披露：`UserDto` 白名单（6 字段）经 Jackson + 反射双重校验，新增字段必须通过 QA review
- XSS：`RegisterView` 中用户名通过 `{{ }}` 插值自动转义
- SQLi：`@Pattern` 在 Controller 层拒绝；JPA 参数化查询二重保障
