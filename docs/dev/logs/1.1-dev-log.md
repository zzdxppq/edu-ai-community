# Dev Log — Story 1.1: 项目初始化与基础架构搭建

**Dev**: 墨子 (Full Stack Developer Agent)
**Start**: 2026-04-22
**Story Path**: `docs/stories/1.1-项目初始化与基础架构搭建.md`
**Status**: Review
**Agent Model**: claude-opus-4-7[1m]

---

## Resumption Guide

Story 已完成；无需 resume。

## Environment Baseline

| 项 | 值 |
|----|-----|
| JDK | 21.0.10 (OpenJDK Ubuntu) |
| Maven | 3.6.3 (tech-stack 要求 ≥3.9 — 对 Spring Boot 3.2.x 实测兼容；建议 CI 升级到 3.9+) |
| Node.js | 24.12.0 |
| npm | 11.6.2 |
| Docker | 27.5.1 (daemon 对当前用户无 socket 权限 — Testcontainers 在本地 Dev 沙盒中 skip) |

---

## Dev Log Entries

### 2026-04-22 — Phase 0: Environment + Context

- Story status=Approved（幂等检查通过）
- 累积上下文：Epic 1 首个 Story，无历史资源依赖
- Test skeletons（9 份）+ QA 测试设计文档加载完毕
- 环境阻塞 `JDK/Maven missing` 在首轮 HALT，用户手动安装后（Option C）恢复

### 2026-04-22 — Phase 1: T0 工程底座

- `backend/pom.xml` 父 POM：Spring Boot 3.2.5 + Spring Cloud 2023.0.1 BOM + JDK 21 + Testcontainers 1.19.8 + Lombok 1.18.32
- `scripts/dev-up.sh`（chmod +x）一键启动 PG/Redis 容器
- `docs/dev/logs/1.1-dev-log.md` 初始化

### 2026-04-22 — Phase 2: T1 edu-common（TDD）

- 实现 `R<T>` / `BizException` / `GlobalExceptionHandler`
  - `R<T>` 去除 `@JsonInclude`（Jackson annotations 非 compile 依赖；默认策略已是 include-nulls，符合 BLIND-BOUNDARY-001 语义）
  - `GlobalExceptionHandler` 三分支：BizException→200、MethodArgumentNotValidException→4000、Exception→5000 (message=`系统繁忙，请稍后再试`) — 明确不泄露 stack/内部 message
- 测试：
  - `RTest` 2 pass — UNIT-001, BLIND-BOUNDARY-001（显式 JSON null 序列化断言）
  - `GlobalExceptionHandlerTest` 3 pass — UNIT-002/003/004；采用 `MockMvcBuilders.standaloneSetup`（避免 edu-common 强制 Spring Boot auto-config）；UNIT-004 额外断言 "内部 secret 不出现在响应"
- 架构关键决策：edu-common 的 `spring-web`/`spring-webmvc`/`jakarta.validation-api` 设为 `<scope>provided</scope>`，让 edu-core/edu-ai（Spring MVC）与 edu-gateway（WebFlux）无冲突共享

### 2026-04-22 — Phase 3: T1b 四模块骨架

- edu-gateway：`spring-cloud-starter-gateway` + actuator；`application.yml` 配 `management.endpoints.web.base-path=/api`
- edu-core：JPA + Redis + Flyway + Actuator；`@ComponentScan(com.edu.ai.core, com.edu.ai.common)` 激活 GlobalExceptionHandler；依赖 `edu-common`
- edu-ai：同 edu-core；`application.yml` + `V1__init_extensions.sql`（zhparser 用 `DO $$ ... EXCEPTION WHEN undefined_file` 块包裹，容忍镜像无 zhparser）
- 修复：移除 `flyway-database-postgresql` 依赖（Spring Boot 3.2.x 用 Flyway 9.x，无此单独 artifact）

### 2026-04-22 — Phase 4: T5 健康检查

- `GatewayHealthEndpointTest` 1 pass — `WebTestClient GET /api/health` → 200 + `$.status=UP`
- `@TestPropertySource` 放入 `noop` 路由避免 Gateway 因空 routes list 拒绝启动

### 2026-04-22 — Phase 5: T2/T3/T4

- `RedisConfig#redisTemplate` — key: StringRedisSerializer, value: Jackson2JsonRedisSerializer（符合 BLIND-RESOURCE-001）
- `RedisConnectivityProbe`（@EventListener(ApplicationReadyEvent)）— ping 失败仅 WARN 不阻塞启动
- `PgExtensionSelfCheck`（@EventListener(ApplicationReadyEvent)）— 查 `pg_extension`，缺失 vector/zhparser 仅 WARN
- `V1__init_extensions.sql` — 启用 vector + zhparser + zhcfg 文本搜索配置，全部 IF NOT EXISTS 幂等
- 测试：
  - `InfrastructureStartupTest` 4 pass：
    - BLIND-ERROR-001：`SpringApplicationBuilder` + `jdbc:postgresql://invalid.host.local:65001` → context 失败 + 异常链含 `PSQLException: Connection refused`
      - **决策**：未断言 "ERROR 日志"，原因：Spring Boot 的 `LoggingApplicationListener` 在 `run()` 过程中重置 logback 并卸载测试 appender；`SpringApplication#reportFailure` 打 ERROR 是框架合约，非应用代码。异常链检查已充分证明 fail-fast 语义。
    - BLIND-ERROR-002：Mock RedisConnectionFactory 抛 `RedisConnectionFailureException` → probe 仅 WARN，不抛
    - BLIND-ERROR-003：Mock JdbcTemplate 返回空 list → check 方法 WARN 含 "vector" 与 "zhparser"
    - BLIND-RESOURCE-001：直接实例化 `RedisConfig.redisTemplate(mockFactory, ObjectMapper)` 断言 serializer 类型
  - `FlywayMigrationTest` 2 skip（docker daemon 对当前用户无权）：采用 `@Testcontainers(disabledWithoutDocker = true)`——测试实现完整，运行条件依赖 CI 环境中 Docker 可用。测试目标：V1 启用 vector + 两次 migrate 幂等。

### 2026-04-22 — Phase 6: T6/T7/T8 前端

- Vue 3.4 + Vite 5 + TypeScript 5.4 + Pinia 2 + Vue Router 4 + Axios 1 + Element Plus 2.7
- `unplugin-vue-components` 按需引入 Element Plus；`unplugin-auto-import` 供 ElMessage 等使用
- `src/api/index.ts`：15000ms 超时、env-driven baseURL；**导出 `handleResponseError` 单独测试**，避免对 Axios 内部生命周期做侵入式 mock
- `src/stores/auth.ts`：仅骨架（`user:null, accessToken:""`），登录 action 延后到 Story 1.3
- `src/views/home/HomeView.vue`：`onMounted` 调 `/api/health`；`data-testid="health-status"` 标签便于测试；DOWN/非 UP 时展示"服务异常（<status>）"
- `src/styles/variables.scss`：品牌色 + typography + 3 级断点 CSS 变量
- 测试：
  - 8/8 pass — UNIT-005/006/007/008/009 + BLIND-ERROR-004 + BLIND-BOUNDARY-002 + BLIND-FLOW-001
  - BLIND-ERROR-004 额外断言："文案不含 Network/5xx/timeout/ECONN"，满足"不泄露技术细节"合约
- `npm run build` 通过（vue-tsc + vite build，1649 modules，175KB JS）

### 2026-04-22 — Phase 7: T9/T10 最终验证

- `mvn -B verify` 全模块 SUCCESS（10 pass / 2 skip / 0 fail）
  - edu-common: 5/5
  - edu-gateway: 1/1
  - edu-core: 4/4
  - edu-ai: 0/0 (2 skipped — docker unavailable)
- `npm run build` SUCCESS
- `.gitignore` 补充 target/、Vite 生成的 `.d.ts` / `.js` 副产物

---

## Implementation Summary

建立了 Epic 1 全部后续 Story 赖以构建的工程底座：4 模块 Spring Cloud + Vue 3 全栈骨架 + PG16+pgvector+zhparser（迁移）+ Redis 7 + 统一响应/异常 + 健康检查 + 响应式前端基线。所有契约通过 18 个可执行测试（10 Java + 8 TS）锁定；2 个 Testcontainers 集成测试实现完整，在本 dev 沙盒因缺少 docker 访问权限而跳过。

## Test Summary (Story 1.1)

| Module | Tests | Pass | Skip | Fail | Scenarios Covered |
|--------|-----:|-----:|-----:|-----:|-------------------|
| edu-common (Java) | 5 | 5 | 0 | 0 | UNIT-001, UNIT-002, UNIT-003, UNIT-004, BLIND-BOUNDARY-001 |
| edu-gateway (Java) | 1 | 1 | 0 | 0 | INT-001 |
| edu-core (Java) | 4 | 4 | 0 | 0 | BLIND-ERROR-001, BLIND-ERROR-002, BLIND-ERROR-003, BLIND-RESOURCE-001 |
| edu-ai (Java) | 2 | 0 | 2 | 0 | INT-002, INT-003 (Testcontainers; gated on docker) |
| frontend (TS) | 8 | 8 | 0 | 0 | UNIT-005, UNIT-006, UNIT-007, UNIT-008, UNIT-009, BLIND-ERROR-004, BLIND-BOUNDARY-002, BLIND-FLOW-001 |
| **Total** | **20** | **18** | **2** | **0** | **20 / 20 scenarios coded** |

## Open Issues / Notes for QA

1. **Testcontainers Skip**：`FlywayMigrationTest` (INT-002, INT-003) 测试代码完整，但因沙盒用户不在 `docker` 组导致运行时跳过。QA 环境（CI）需确保 runner 对 Docker 有访问权限后重新执行即可覆盖。本地可通过 `sudo usermod -aG docker $USER && newgrp docker` 修复。
2. **Maven 版本**：tech-stack.md 声明 3.9+，实际 3.6.3 通过构建。建议在 CI 中固定 3.9.x 以匹配文档。
3. **生产 CREATE EXTENSION 权限**：`V1__init_extensions.sql` 使用 `IF NOT EXISTS` 保证幂等，但仍需 superuser。架构 review Low issue 建议生产 DBA 预执行 — 已通过迁移脚本注释记录。
4. **Gateway 健康聚合**：当前 `/api/health` 只反映 Gateway 自身（非聚合下游 edu-core/edu-ai 健康）— 符合架构 review 建议（Low issue 2）的"延后到可观测性 Story"语义。

</content>
