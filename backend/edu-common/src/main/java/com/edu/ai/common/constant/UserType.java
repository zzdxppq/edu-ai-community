package com.edu.ai.common.constant;

/**
 * User classification. Registration always creates {@link #USER};
 * {@link #ADMIN} is provisioned by the operations backend (Epic 6).
 */
public enum UserType {
    USER,
    ADMIN
}
